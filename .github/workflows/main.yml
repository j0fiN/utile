

name: CI-utile


on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]


jobs:

  build:
    runs-on: windows-latest
    
    steps:
    - uses: actions/checkout@v2

    # Introduction
    - name: utile building
      run: echo utile will be ready soon...

    # Runs test
    - name: Running tests
      run: |
        echo Building...
        cd tests
        python -m unittest
